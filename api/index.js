const { getDocs, getAllDocs } = require('./docs')
const { getStyleguide, getAllStyleguides } = require('./styleguide')

module.exports = {
  getDocs,
  getAllDocs,
  getStyleguide,
  getAllStyleguides
}
